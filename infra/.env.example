# Copy to .env. Optional — it holds test switches only.
#
# The app's own settings are NOT here: they live in
# deployables/chess-cli/config/chess_cli.yaml and are parsed into a dataclass.
# Nothing in the app reads the environment.

# 1 to include the deep perft counts, which take a few minutes.
CHESS_SLOW_TESTS=0
