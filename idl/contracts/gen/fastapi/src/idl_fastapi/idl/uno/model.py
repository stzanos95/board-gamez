# generated by datamodel-codegen:
#   filename:  openapi.yaml

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field

from ..game import model


class Card(BaseModel):
    """
    One card.

     Carries no identity. Two cards with the same kind, colour and number are
     interchangeable. `number` is read only when the kind is NUMBER, so a zero
     there is the 0 card and nowhere else means anything.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    kind: Literal['CARD_KIND_UNSPECIFIED', 'CARD_KIND_NUMBER', 'CARD_KIND_SKIP', 'CARD_KIND_REVERSE', 'CARD_KIND_DRAW_TWO', 'CARD_KIND_WILD', 'CARD_KIND_WILD_DRAW_FOUR'] | None = (
        None
    )
    color: Literal['CARD_COLOR_UNSPECIFIED', 'CARD_COLOR_RED', 'CARD_COLOR_YELLOW', 'CARD_COLOR_GREEN', 'CARD_COLOR_BLUE'] | None = (
        None
    )
    number: int | None = None


class CardDraw(BaseModel):
    """
    Taking the top card of the draw pile into the hand.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )


class CardPlay(BaseModel):
    """
    Playing one card from the hand onto the discard pile.

     `chosen_color` is set exactly when the card is a wild, and names the colour
     the next player must follow.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    card: Card | None = None
    chosen_color: (
        Literal[
            'CARD_COLOR_UNSPECIFIED',
            'CARD_COLOR_RED',
            'CARD_COLOR_YELLOW',
            'CARD_COLOR_GREEN',
            'CARD_COLOR_BLUE',
        ]
        | None
    ) = Field(default=None, alias='chosenColor')


class HandCard(BaseModel):
    """
    One card in the viewer's hand, and whether it may be played now.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    card: Card | None = None
    is_playable: bool | None = Field(default=None, alias='isPlayable')


class TurnPass(BaseModel):
    """
    Ending the turn without playing, allowed only after a draw.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )


class UnoAction(BaseModel):
    """
    One thing a player does: play a card, draw a card, or pass.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    play: CardPlay | None = None
    draw: CardDraw | None = None
    pass_: TurnPass | None = Field(default=None, alias='pass')


class UnoPlayerSummary(BaseModel):
    """
    What everyone may know about one participant.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    participant: int | None = None
    card_count: int | None = Field(default=None, alias='cardCount')
    has_withdrawn: bool | None = Field(default=None, alias='hasWithdrawn')


class UnoResult(BaseModel):
    """
    How a finished game came out.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    winner: int | None = None
    reason: Literal['UNO_RESULT_REASON_UNSPECIFIED', 'UNO_RESULT_REASON_HAND_EMPTIED', 'UNO_RESULT_REASON_OTHERS_WITHDREW'] | None = (
        None
    )


class UnoSeat(BaseModel):
    """
    One seat at a UNO table.

     A seat's number is its place in the turn order, and a seat carries nothing
     else.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    number: int | None = None
    status: Literal['SEAT_STATUS_UNSPECIFIED', 'SEAT_STATUS_OPEN', 'SEAT_STATUS_OCCUPIED'] | None = (
        None
    )
    player_id: str | None = Field(default=None, alias='playerId')


class UnoSeatChoice(BaseModel):
    """
    One seat a player may take right now.

     Taken unchanged.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    number: int | None = None


class UnoSeatChoiceCollection(BaseModel):
    """
    Every seat one player may take.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    uno_seat_choice_items: list[UnoSeatChoice] | None = Field(
        default=None, alias='unoSeatChoiceItems'
    )


class UnoTable(BaseModel):
    """
    A table UNO is played at.

     `version` is the table's, and a choice is taken against it.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    id: str | None = None
    status: Literal['TABLE_STATUS_UNSPECIFIED', 'TABLE_STATUS_WAITING', 'TABLE_STATUS_IN_PROGRESS', 'TABLE_STATUS_FINISHED', 'TABLE_STATUS_ABANDONED'] | None = (
        None
    )
    seats: list[UnoSeat] | None = None
    player_ids: list[str] | None = Field(default=None, alias='playerIds')
    version: str | None = None


class UnoView(BaseModel):
    """
    The game projected for one viewer.

     `hand` is the viewer's own cards and is empty for someone who is not
     playing. `may_draw` and `may_pass` are true only for the viewer whose turn
     it is; every card's `is_playable` is false for anyone else. Nothing here
     decides an action; the rules do, and this says what they would accept.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    players: list[UnoPlayerSummary] | None = None
    hand: list[HandCard] | None = None
    top_card: Card | None = Field(default=None, alias='topCard')
    active_color: (
        Literal[
            'CARD_COLOR_UNSPECIFIED',
            'CARD_COLOR_RED',
            'CARD_COLOR_YELLOW',
            'CARD_COLOR_GREEN',
            'CARD_COLOR_BLUE',
        ]
        | None
    ) = Field(default=None, alias='activeColor')
    direction: Literal['PLAY_DIRECTION_UNSPECIFIED', 'PLAY_DIRECTION_CLOCKWISE', 'PLAY_DIRECTION_COUNTERCLOCKWISE'] | None = (
        None
    )
    participant_to_act: int | None = Field(default=None, alias='participantToAct')
    draw_pile_count: int | None = Field(default=None, alias='drawPileCount')
    may_draw: bool | None = Field(default=None, alias='mayDraw')
    may_pass: bool | None = Field(default=None, alias='mayPass')
    drawn_card: Card | None = Field(default=None, alias='drawnCard')
    result: UnoResult | None = None


class UnoSeatResult(BaseModel):
    """
    What happened to a seat being taken, and the table as its taker may now see it.

     The table is carried whatever the outcome, and is unset only when no table
     has the id the choice named.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    outcome: Literal['SEAT_OUTCOME_UNSPECIFIED', 'SEAT_OUTCOME_TAKEN', 'SEAT_OUTCOME_TABLE_NOT_FOUND', 'SEAT_OUTCOME_NOT_OFFERED', 'SEAT_OUTCOME_VERSION_MOVED', 'SEAT_OUTCOME_VACATED', 'SEAT_OUTCOME_LEFT', 'SEAT_OUTCOME_NOT_SEATED', 'SEAT_OUTCOME_NOT_AT_TABLE', 'SEAT_OUTCOME_CREATED', 'SEAT_OUTCOME_JOINED', 'SEAT_OUTCOME_ALREADY_AT_TABLE', 'SEAT_OUTCOME_NOT_ACCEPTING_PLAYERS', 'SEAT_OUTCOME_GAME_NOT_HOSTED', 'SEAT_OUTCOME_SEAT_COUNT_NOT_ALLOWED'] | None = (
        None
    )
    table: UnoTable | None = None


class UnoSession(BaseModel):
    """
    One game of UNO being played, projected for one viewer.

     The id is the id of the table the game is played at, so a table holds one
     game at a time.

     `participant` is the viewer's number in the game, and 0 for someone who is
     not playing.

     `version` counts writes to the game. An action is built against the version
     it was read at, and a session is rendered only when its version is above the
     version showing.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    id: str | None = None
    view: UnoView | None = None
    participant: int | None = None
    last_command_id: str | None = Field(default=None, alias='lastCommandId')
    version: str | None = None
    participant_statuses: list[model.ParticipantStatus] | None = Field(
        default=None, alias='participantStatuses'
    )


class ActionResult(BaseModel):
    """
    What happened to an action, and the game as its sender may now see it.

     The session is carried whatever the outcome, and is unset only when no game is
     being played at the table the action named.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    outcome: Literal['COMMAND_OUTCOME_UNSPECIFIED', 'COMMAND_OUTCOME_APPLIED', 'COMMAND_OUTCOME_ALREADY_APPLIED', 'COMMAND_OUTCOME_SESSION_NOT_FOUND', 'COMMAND_OUTCOME_NOT_A_PARTICIPANT', 'COMMAND_OUTCOME_GAME_OVER', 'COMMAND_OUTCOME_VERSION_MOVED', 'COMMAND_OUTCOME_OUT_OF_TURN', 'COMMAND_OUTCOME_ILLEGAL_ACTION'] | None = (
        None
    )
    session: UnoSession | None = None
