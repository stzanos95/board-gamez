# generated by datamodel-codegen:
#   filename:  openapi.yaml

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field

from ..game import model


class CastlingRight(BaseModel):
    """
    One side's claim to one of its two castles.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    color: Literal['COLOR_UNSPECIFIED', 'COLOR_WHITE', 'COLOR_BLACK'] | None = None
    side: Literal['CASTLING_SIDE_UNSPECIFIED', 'CASTLING_SIDE_KINGSIDE', 'CASTLING_SIDE_QUEENSIDE'] | None = (
        None
    )


class CastlingRights(BaseModel):
    """
    Which castles remain available.

     A right survives until the king or the matching rook moves, or that rook is
     captured. It does not say whether the castle is legal now: that also depends
     on occupancy and on which squares the enemy attacks.

     The list is a set. Order carries nothing, and a right appears at most once.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    available: list[CastlingRight] | None = None


class ChessPlayer(BaseModel):
    """
    Which side one participant plays.

     A participant is known here by number and nothing else. A name is joined in by
     whoever draws the game.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    color: Literal['COLOR_UNSPECIFIED', 'COLOR_WHITE', 'COLOR_BLACK'] | None = None
    participant: int | None = None


class ChessSeat(BaseModel):
    """
    One seat at a chess table, with the side it plays opened.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    number: int | None = None
    status: Literal['SEAT_STATUS_UNSPECIFIED', 'SEAT_STATUS_OPEN', 'SEAT_STATUS_OCCUPIED'] | None = (
        None
    )
    player_id: str | None = Field(default=None, alias='playerId')
    color: Literal['COLOR_UNSPECIFIED', 'COLOR_WHITE', 'COLOR_BLACK'] | None = None


class ChessSeatChoice(BaseModel):
    """
    One seat a player may take right now, and the side they would play in it.

     Taken unchanged: the seat and the side are a pair.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    number: int | None = None
    color: Literal['COLOR_UNSPECIFIED', 'COLOR_WHITE', 'COLOR_BLACK'] | None = None


class ChessSeatChoiceCollection(BaseModel):
    """
    Every seat one player may take.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    chess_seat_choice_items: list[ChessSeatChoice] | None = Field(
        default=None, alias='chessSeatChoiceItems'
    )


class ChessTable(BaseModel):
    """
    A table chess is played at, with every seat's side opened.

     `version` is the table's, and a choice is taken against it.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    id: str | None = None
    status: Literal['TABLE_STATUS_UNSPECIFIED', 'TABLE_STATUS_WAITING', 'TABLE_STATUS_IN_PROGRESS', 'TABLE_STATUS_FINISHED', 'TABLE_STATUS_ABANDONED'] | None = (
        None
    )
    seats: list[ChessSeat] | None = None
    player_ids: list[str] | None = Field(default=None, alias='playerIds')
    version: str | None = None


class GameResult(BaseModel):
    """
    How a game finished.

     Exactly one of status and resigning_player is set. A game decided on the board
     carries the status that ended it; a game decided by resignation carries the
     player who resigned. winner is absent for a draw.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    outcome: Literal['GAME_OUTCOME_UNSPECIFIED', 'GAME_OUTCOME_WHITE_WINS', 'GAME_OUTCOME_BLACK_WINS', 'GAME_OUTCOME_DRAW'] | None = (
        None
    )
    winner: ChessPlayer | None = None
    status: Literal['GAME_STATUS_UNSPECIFIED', 'GAME_STATUS_IN_PROGRESS', 'GAME_STATUS_CHECK', 'GAME_STATUS_CHECKMATE', 'GAME_STATUS_STALEMATE', 'GAME_STATUS_DRAW_BY_FIFTY_MOVE_RULE', 'GAME_STATUS_DRAW_BY_REPETITION', 'GAME_STATUS_DRAW_BY_INSUFFICIENT_MATERIAL'] | None = (
        None
    )
    resigning_player: ChessPlayer | None = Field(default=None, alias='resigningPlayer')


class Occupant(BaseModel):
    """
    Whose piece stands on a square, and what type.

     Carries no identity. Two knights of the same colour are interchangeable.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    color: Literal['COLOR_UNSPECIFIED', 'COLOR_WHITE', 'COLOR_BLACK'] | None = None
    piece_type: (
        Literal[
            'PIECE_TYPE_UNSPECIFIED',
            'PIECE_TYPE_PAWN',
            'PIECE_TYPE_KNIGHT',
            'PIECE_TYPE_BISHOP',
            'PIECE_TYPE_ROOK',
            'PIECE_TYPE_QUEEN',
            'PIECE_TYPE_KING',
        ]
        | None
    ) = Field(default=None, alias='pieceType')


class PlayerRoster(BaseModel):
    """
    The two players, addressable by colour.

     white.color is always COLOR_WHITE, and black.color COLOR_BLACK.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    white: ChessPlayer | None = None
    black: ChessPlayer | None = None


class Resignation(BaseModel):
    """
    Giving up the game.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )


class Square(BaseModel):
    """
    One of the sixty-four squares, named by its file and rank.

     The algebraic spelling "e4" is derived from this, and is not carried.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    file: Literal['FILE_UNSPECIFIED', 'FILE_A', 'FILE_B', 'FILE_C', 'FILE_D', 'FILE_E', 'FILE_F', 'FILE_G', 'FILE_H'] | None = (
        None
    )
    rank: Literal['RANK_UNSPECIFIED', 'RANK_1', 'RANK_2', 'RANK_3', 'RANK_4', 'RANK_5', 'RANK_6', 'RANK_7', 'RANK_8'] | None = (
        None
    )


class SquareOccupant(BaseModel):
    """
    One square and the piece standing on it.

     A position travels as a list of these.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    square: Square | None = None
    occupant: Occupant | None = None


class BoardState(BaseModel):
    """
    A complete position: pieces, side to move, castling rights, en-passant target
     and both clocks.

     Only occupied squares appear in occupancy, and order carries nothing.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    occupancy: list[SquareOccupant] | None = None
    side_to_move: Literal['COLOR_UNSPECIFIED', 'COLOR_WHITE', 'COLOR_BLACK'] | None = (
        Field(default=None, alias='sideToMove')
    )
    castling_rights: CastlingRights | None = Field(default=None, alias='castlingRights')
    en_passant_target: Square | None = Field(default=None, alias='enPassantTarget')
    halfmove_clock: int | None = Field(default=None, alias='halfmoveClock')
    fullmove_number: int | None = Field(default=None, alias='fullmoveNumber')


class ChessSeatResult(BaseModel):
    """
    What happened to a seat being taken, and the table as its taker may now see it.

     The table is carried whatever the outcome, and is unset only when no table
     has the id the choice named.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    outcome: Literal['SEAT_OUTCOME_UNSPECIFIED', 'SEAT_OUTCOME_TAKEN', 'SEAT_OUTCOME_TABLE_NOT_FOUND', 'SEAT_OUTCOME_NOT_OFFERED', 'SEAT_OUTCOME_VERSION_MOVED', 'SEAT_OUTCOME_VACATED', 'SEAT_OUTCOME_LEFT', 'SEAT_OUTCOME_NOT_SEATED', 'SEAT_OUTCOME_NOT_AT_TABLE', 'SEAT_OUTCOME_CREATED', 'SEAT_OUTCOME_JOINED', 'SEAT_OUTCOME_ALREADY_AT_TABLE', 'SEAT_OUTCOME_NOT_ACCEPTING_PLAYERS', 'SEAT_OUTCOME_GAME_NOT_HOSTED', 'SEAT_OUTCOME_SEAT_COUNT_NOT_ALLOWED', 'SEAT_OUTCOME_TABLE_FULL'] | None = (
        None
    )
    table: ChessTable | None = None


class CoordinateMove(BaseModel):
    """
    A move as a player names it: the square left, the square reached and, for a
     pawn reaching the last rank, what it becomes.

     Which piece moves, whether it captures and how it castles are not carried.
     They are settled by matching this against the legal moves of the position.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    origin: Square | None = None
    destination: Square | None = None
    promotion_type: (
        Literal[
            'PIECE_TYPE_UNSPECIFIED',
            'PIECE_TYPE_PAWN',
            'PIECE_TYPE_KNIGHT',
            'PIECE_TYPE_BISHOP',
            'PIECE_TYPE_ROOK',
            'PIECE_TYPE_QUEEN',
            'PIECE_TYPE_KING',
        ]
        | None
    ) = Field(default=None, alias='promotionType')


class Move(BaseModel):
    """
    One move, complete enough for a board to apply without deriving anything.

     The mover is carried as colour and type.

     captured_square differs from destination for en passant. The rook fields are
     set only for a castle. A field that does not apply is left unset.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    origin: Square | None = None
    destination: Square | None = None
    moving_color: Literal['COLOR_UNSPECIFIED', 'COLOR_WHITE', 'COLOR_BLACK'] | None = (
        Field(default=None, alias='movingColor')
    )
    moving_piece_type: (
        Literal[
            'PIECE_TYPE_UNSPECIFIED',
            'PIECE_TYPE_PAWN',
            'PIECE_TYPE_KNIGHT',
            'PIECE_TYPE_BISHOP',
            'PIECE_TYPE_ROOK',
            'PIECE_TYPE_QUEEN',
            'PIECE_TYPE_KING',
        ]
        | None
    ) = Field(default=None, alias='movingPieceType')
    move_type: (
        Literal[
            'MOVE_TYPE_UNSPECIFIED',
            'MOVE_TYPE_QUIET',
            'MOVE_TYPE_CAPTURE',
            'MOVE_TYPE_DOUBLE_PAWN_PUSH',
            'MOVE_TYPE_EN_PASSANT',
            'MOVE_TYPE_CASTLE',
            'MOVE_TYPE_PROMOTION',
            'MOVE_TYPE_PROMOTION_CAPTURE',
        ]
        | None
    ) = Field(default=None, alias='moveType')
    captured_square: Square | None = Field(default=None, alias='capturedSquare')
    promotion_type: (
        Literal[
            'PIECE_TYPE_UNSPECIFIED',
            'PIECE_TYPE_PAWN',
            'PIECE_TYPE_KNIGHT',
            'PIECE_TYPE_BISHOP',
            'PIECE_TYPE_ROOK',
            'PIECE_TYPE_QUEEN',
            'PIECE_TYPE_KING',
        ]
        | None
    ) = Field(default=None, alias='promotionType')
    castling_side: (
        Literal[
            'CASTLING_SIDE_UNSPECIFIED',
            'CASTLING_SIDE_KINGSIDE',
            'CASTLING_SIDE_QUEENSIDE',
        ]
        | None
    ) = Field(default=None, alias='castlingSide')
    rook_origin: Square | None = Field(default=None, alias='rookOrigin')
    rook_destination: Square | None = Field(default=None, alias='rookDestination')


class PositionKey(BaseModel):
    """
    What makes two positions the same for the repetition rule.

     The same pieces on the same squares, the same side to move, the same castling
     rights and the same en-passant square. The clocks are excluded: they differ
     between repetitions of one position.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    occupancy: list[SquareOccupant] | None = None
    side_to_move: Literal['COLOR_UNSPECIFIED', 'COLOR_WHITE', 'COLOR_BLACK'] | None = (
        Field(default=None, alias='sideToMove')
    )
    castling_rights: CastlingRights | None = Field(default=None, alias='castlingRights')
    en_passant_target: Square | None = Field(default=None, alias='enPassantTarget')


class ChessAction(BaseModel):
    """
    One thing a player does: play a move, or resign.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    move: CoordinateMove | None = None
    resignation: Resignation | None = None


class ChessTurn(BaseModel):
    """
    One move that was played, with its written form.

     The notation is standard algebraic — "Nf3", "exd5", "O-O", "e8=Q#" — never
     coordinate text. It is stored because rendering it needs the position the move
     was played from, which a reader of the history no longer has.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    number: int | None = None
    player: ChessPlayer | None = None
    move: Move | None = None
    notation: str | None = None
    resulting_status: (
        Literal[
            'GAME_STATUS_UNSPECIFIED',
            'GAME_STATUS_IN_PROGRESS',
            'GAME_STATUS_CHECK',
            'GAME_STATUS_CHECKMATE',
            'GAME_STATUS_STALEMATE',
            'GAME_STATUS_DRAW_BY_FIFTY_MOVE_RULE',
            'GAME_STATUS_DRAW_BY_REPETITION',
            'GAME_STATUS_DRAW_BY_INSUFFICIENT_MATERIAL',
        ]
        | None
    ) = Field(default=None, alias='resultingStatus')


class ChessTurnHistory(BaseModel):
    """
    The turns played, and the position after each of them.

     position_keys runs one ahead of turns: the opening position counts as visited
     before either player has moved.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    turns: list[ChessTurn] | None = None
    position_keys: list[PositionKey] | None = Field(default=None, alias='positionKeys')


class ChessGame(BaseModel):
    """
    A game in progress: the players, the position, and the moves played.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    roster: PlayerRoster | None = None
    state: BoardState | None = None
    history: ChessTurnHistory | None = None
    legal_moves: list[Move] | None = Field(default=None, alias='legalMoves')
    status: Literal['GAME_STATUS_UNSPECIFIED', 'GAME_STATUS_IN_PROGRESS', 'GAME_STATUS_CHECK', 'GAME_STATUS_CHECKMATE', 'GAME_STATUS_STALEMATE', 'GAME_STATUS_DRAW_BY_FIFTY_MOVE_RULE', 'GAME_STATUS_DRAW_BY_REPETITION', 'GAME_STATUS_DRAW_BY_INSUFFICIENT_MATERIAL'] | None = (
        None
    )
    resigning_color: (
        Literal['COLOR_UNSPECIFIED', 'COLOR_WHITE', 'COLOR_BLACK'] | None
    ) = Field(default=None, alias='resigningColor')
    result: GameResult | None = None


class ChessSession(BaseModel):
    """
    One game of chess being played, projected for one viewer.

     The id is the id of the table the game is played at, so a table holds one
     game at a time.

     `color` is the side the viewer plays, and is unset for someone who is not
     playing. Which side may act next is read from the game's own state.

     `version` counts writes to the game. An action is built against the version
     it was read at, and a session is rendered only when its version is above the
     version showing.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    id: str | None = None
    game: ChessGame | None = None
    color: Literal['COLOR_UNSPECIFIED', 'COLOR_WHITE', 'COLOR_BLACK'] | None = None
    last_command_id: str | None = Field(default=None, alias='lastCommandId')
    version: str | None = None
    participant_statuses: list[model.ParticipantStatus] | None = Field(
        default=None, alias='participantStatuses'
    )


class ActionResult(BaseModel):
    """
    What happened to an action, and the game as its sender may now see it.

     The session is carried whatever the outcome, and is unset only when no game is
     being played at the table the action named.
    """

    model_config = ConfigDict(
        populate_by_name=True,
    )
    outcome: Literal['COMMAND_OUTCOME_UNSPECIFIED', 'COMMAND_OUTCOME_APPLIED', 'COMMAND_OUTCOME_ALREADY_APPLIED', 'COMMAND_OUTCOME_SESSION_NOT_FOUND', 'COMMAND_OUTCOME_NOT_A_PARTICIPANT', 'COMMAND_OUTCOME_GAME_OVER', 'COMMAND_OUTCOME_VERSION_MOVED', 'COMMAND_OUTCOME_OUT_OF_TURN', 'COMMAND_OUTCOME_ILLEGAL_ACTION'] | None = (
        None
    )
    session: ChessSession | None = None
