# Generated by ./idl/scripts/generate.sh from idl/scripts/templates/.
# Edit the template, not this file.

[project]
name = "idl-fastapi"
version = "0.1.0"
description = "Generated FastAPI bindings: the models and routers each service declares."
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.13",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/idl_fastapi"]
