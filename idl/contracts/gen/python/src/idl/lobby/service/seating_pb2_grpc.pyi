"""
@generated by mypy-protobuf.  Do not edit manually!
isort:skip_file
How a game seats its players."""

import abc
import collections.abc
import grpc
import grpc.aio
import idl.lobby.dto.seating_pb2
import typing

_T = typing.TypeVar("_T")

class _MaybeAsyncIterator(collections.abc.AsyncIterator[_T], collections.abc.Iterator[_T], metaclass=abc.ABCMeta): ...

class _ServicerContext(grpc.ServicerContext, grpc.aio.ServicerContext):  # type: ignore[misc, type-arg]
    ...

class SeatingServiceStub:
    """What a game answers about its seats, whatever game it is.

    Implemented once per game and reached under the game type it was configured
    for. Every call carries the table it acts on, so an implementation keeps
    nothing between calls and writes no table of its own.

    Nothing outside the platform asks a game how it seats people, so no HTTP path
    is declared here.
    """

    def __init__(self, channel: typing.Union[grpc.Channel, grpc.aio.Channel]) -> None: ...
    ListSeatChoice: grpc.UnaryUnaryMultiCallable[
        idl.lobby.dto.seating_pb2.ListSeatChoiceRequest,
        idl.lobby.dto.seating_pb2.ListSeatChoiceResponse,
    ]

class SeatingServiceAsyncStub:
    """What a game answers about its seats, whatever game it is.

    Implemented once per game and reached under the game type it was configured
    for. Every call carries the table it acts on, so an implementation keeps
    nothing between calls and writes no table of its own.

    Nothing outside the platform asks a game how it seats people, so no HTTP path
    is declared here.
    """

    ListSeatChoice: grpc.aio.UnaryUnaryMultiCallable[
        idl.lobby.dto.seating_pb2.ListSeatChoiceRequest,
        idl.lobby.dto.seating_pb2.ListSeatChoiceResponse,
    ]

class SeatingServiceServicer(metaclass=abc.ABCMeta):
    """What a game answers about its seats, whatever game it is.

    Implemented once per game and reached under the game type it was configured
    for. Every call carries the table it acts on, so an implementation keeps
    nothing between calls and writes no table of its own.

    Nothing outside the platform asks a game how it seats people, so no HTTP path
    is declared here.
    """

    @abc.abstractmethod
    def ListSeatChoice(
        self,
        request: idl.lobby.dto.seating_pb2.ListSeatChoiceRequest,
        context: _ServicerContext,
    ) -> typing.Union[idl.lobby.dto.seating_pb2.ListSeatChoiceResponse, collections.abc.Awaitable[idl.lobby.dto.seating_pb2.ListSeatChoiceResponse]]: ...

def add_SeatingServiceServicer_to_server(servicer: SeatingServiceServicer, server: typing.Union[grpc.Server, grpc.aio.Server]) -> None: ...
